- uses :litijs


- content_for :title do
  Litijs Documentation

- content_for :script do
  :plain 
    var file = ({
      litijs: 'scripts/story/litijs.js',
      template: 'scripts/story/template.js',
      plot: 'scripts/story/plot.js',
      story: 'scripts/story/story.js',
      layer: 'scripts/story/layer.js',
    })[window.location.search.slice(1)] || 'scripts/story/story.js';
    
    Litijs($('body'), file, function() {
      if(window.location.hash) {
        var element = document.getElementById('litijs.' + window.location.hash.slice(1));
        if(element) {
          window.scrollTo(0, $(element).offset().top);
        } else debugger;
      }
    });
-# vim: set sw=2 ts=2 expandtab :
